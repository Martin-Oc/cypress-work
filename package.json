{
  "name": "cypressautomation",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "cypress-cucumber-preprocessor": {
    "nonGlobalStepDefinitions": true
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "cypress": "^8.1.0",
    "cypress-cucumber-preprocessor": "^4.3.1"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "open:cy": "npx cypress open",
    "cucumber:test": "npx cypress run --headed --spec ./cypress/integration/Feature_Files/cucumber.feature --browser chrome",
    "first": "cypress run --browser chrome --headed --spec ./cypress/integration/1-getting-started/todo.spec.js"
  }
  
}
